# Quicksand

app: ported applications.

build_all.sh: the building script.

test.sh: the testing script.

cloudlab.profile: the cloudlab profile to run Quicksand.

exp: the code and scripts for running experiments.

inc: headers and inlined implementation files.

src: source files.

test: testing files.
